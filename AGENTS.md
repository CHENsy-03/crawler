# AGENTS.md



## 1. 文件目的



本文件定义 AI 编程助手、Codex 及人工开发者在 `workspace/crawler` 通用采集平台中的工作规则。



本项目属于总仓库：



```text

E:\\AI\_Projects\\Codex

```



项目路径为：



```text

E:\\AI\_Projects\\Codex\\workspace\\crawler

```



本文件只约束 `workspace/crawler` 项目及其子目录，不负责规定其他项目的开发方式。



所有参与本项目的 AI 和开发者必须遵守本文件，不得在未检查真实代码、未确认当前任务或未执行必要测试的情况下修改项目。



---



## 2. 规则适用范围



本文件适用于以下内容：



```text

workspace/crawler/

├── main.py

├── api/

├── config/

├── core/

├── crawler/

├── dedup/

├── extractor/

├── frontend/

├── go-spider/

├── httpx/

├── monitor/

├── plugins/

├── scheduler/

├── storage/

├── tests/

├── utils/

├── docs/

├── docker-compose.yml

└── 其他属于本采集平台的文件

```



项目中的 Python、Go、HTML、JavaScript、SQL、JSON、YAML、Docker、Shell、PowerShell 和 Markdown 文件均受本规则约束。



如果将来某个子目录存在更具体的 `AGENTS.md`，则该子目录中的工作还必须遵守更具体的规则。



---



## 3. 规则优先级



执行任务时，按照以下顺序理解和应用规则：



1\. 用户当前明确提出的要求；

2\. 总仓库 `E:\\AI\_Projects\\Codex\\AGENTS.md`；

3\. 当前项目 `workspace/crawler/AGENTS.md`；

4\. 当前目录下更具体的子级 `AGENTS.md`；

5\. `docs/DEVELOPMENT\_RULES.md`；

6\. `docs/SYSTEM\_ARCHITECTURE.md`；

7\. 当前 `docs/TASK.md`；

8\. 现有代码、测试、接口契约和数据库结构。



如果这些内容发生冲突：



* 不得自行选择对业务影响更大的方案；

* 必须指出具体冲突；

* 必须说明涉及的文件、接口和风险；

* 在获得用户确认前，不执行不可逆或大范围修改。



架构文档描述长期边界，任务文档描述本次工作的具体范围。任务文档不得暗中改变已经确认的系统架构。



---



## 4. 开始任务前必须阅读的内容



每次开始修改代码前，必须依次检查：



```text

AGENTS.md

docs/TASK.md

docs/SYSTEM\_ARCHITECTURE.md

docs/DEVELOPMENT\_RULES.md

docs/decisions/

README.md

```



如果任务涉及某个具体模块，还必须检查：



* 该模块的现有代码；

* 该模块的调用方；

* 该模块调用的下游代码；

* 对应测试；

* 对应配置；

* 对应接口模型；

* 对应数据库表或迁移；

* 相关 ADR。



不得仅根据文件名、目录名、旧对话或架构设想推断实现方式。



必须以当前工作区中的真实代码为准。



---



## 5. Git 状态检查



开始任务前必须在总仓库根目录或项目目录执行：



```powershell

git branch --show-current

git status --short

git diff --stat

```



需要确认：



* 当前所在分支；

* 是否存在用户尚未提交的修改；

* 当前任务是否使用正确分支；

* 是否有与本任务无关的变更；

* 是否存在未跟踪文件。



如果发现已有修改：



* 默认认为这些修改属于用户；

* 不得擅自删除、覆盖或回滚；

* 不得执行 `git reset --hard`；

* 不得执行会覆盖用户文件的还原操作；

* 必须绕开无关修改；

* 如果修改范围重叠，必须先说明冲突再继续。



推荐分支命名：



```text

feat/crawler-简短名称

fix/crawler-简短名称

refactor/crawler-简短名称

test/crawler-简短名称

docs/crawler-简短名称

chore/crawler-简短名称

```



一个分支应尽量只对应一个明确任务。



---



## 6. 单任务执行原则



项目使用：



```text

docs/TASK.md

```



记录当前正在执行的唯一任务。



任何开发工作开始前，必须确认 `docs/TASK.md` 至少明确：



* 任务编号；

* 任务名称；

* 任务类型；

* 当前状态；

* 任务背景；

* 当前问题；

* 任务目标；

* 非目标；

* 允许修改范围；

* 禁止修改范围；

* 实施步骤；

* 测试要求；

* 验收标准；

* 回滚方案。



一次只执行一个任务。



不得在执行 `TASK-001` 时顺便实施 `TASK-002`、`TASK-003` 或其他后续任务。



发现其他问题时，应：



1\. 记录问题；

2\. 说明影响；

3\. 将其列入阻塞问题或后续任务建议；

4\. 不在当前任务中顺手修复，除非它直接阻塞当前任务且已得到用户确认。



---



## 7. 任务状态规则



`docs/TASK.md` 中的任务状态允许使用：



```text

draft

ready

in\_progress

blocked

review

completed

cancelled

```



状态含义：



| 状态            | 含义                 |

| ------------- | ------------------ |

| `draft`       | 任务仍在编写，不能开始修改代码    |

| `ready`       | 范围和验收标准已明确，可以开始    |

| `in\_progress` | 正在实施               |

| `blocked`     | 因依赖、权限、设计冲突或环境问题暂停 |

| `review`      | 实施完成，等待检查或验收       |

| `completed`   | 已满足验收标准并记录真实测试结果   |

| `cancelled`   | 任务取消，不再继续          |



不得在测试未执行、失败未记录或验收标准未核对时，将任务标记为 `completed`。



---



## 8. 修改范围控制



AI 只能修改当前 `docs/TASK.md` 明确允许修改的文件和目录。



修改前必须回答以下问题：



1\. 这个文件是否在允许修改范围内？

2\. 修改是否直接服务于当前任务目标？

3\. 是否会改变公共接口？

4\. 是否会改变数据库结构？

5\. 是否会改变 Python 与 Go 的职责边界？

6\. 是否会影响其他在研项目？

7\. 是否存在更小的修改方案？

8\. 是否能够通过测试验证？



如果任务没有授权，不得进行：



* 大规模目录调整；

* 全项目重命名；

* 删除兼容层；

* 删除旧模块；

* 修改公共 API；

* 修改数据库结构；

* 更换核心框架；

* 引入大型依赖；

* 将现有模块整体重写；

* 将临时方案直接定为正式架构；

* 跨项目复制或迁移代码；

* 修改 `workspace/crawler` 之外的项目。



---



## 9. 修改前必须检查真实代码



在提出修复方案或实施改动前，必须检查：



```text

定义位置

调用位置

输入数据

输出数据

错误处理

配置来源

测试覆盖

运行入口

持久化影响

并发影响

```



不得只读取单个文件就声称已经检查“全部代码”。



如果任务需要检查完整调用链，应使用代码搜索确认：



```text

入口

→ API

→ 调度

→ 核心服务

→ 下载或解析

→ 存储

→ 返回结果

```



对于 Python，应重点检查：



```text

import

函数调用

类实例化

异常捕获

配置加载

数据库访问

Redis访问

文件写入

测试替身

```



对于 Go，应重点检查：



```text

package引用

接口实现

handler调用

service调用

repository调用

goroutine

channel

context

锁

事务

错误返回

```



结论必须区分：



* 已从代码确认；

* 已从测试确认；

* 已从配置确认；

* 尚未确认；

* 仅为建议。



---



## 10. 系统总体职责边界



本项目采用 Python 与 Go 协作架构。



总体原则：



```text

Go：平台控制面

Python：采集执行面

```



任何任务都不得未经架构决策记录而重新制造 Python 与 Go 的重复职责。



---



## 11. Python 职责



Python负责：



* 网站能力探测；

* 搜索入口识别；

* 搜索规则生成；

* 搜索请求执行；

* HTML页面下载；

* JSON接口请求；

* 浏览器动态请求探测；

* HTML解析；

* PDF解析；

* Office文档解析；

* 正文提取；

* 内容清洗；

* 关键词拓展；

* 关键词匹配；

* 相关性评分；

* URL标准化；

* URL去重；

* 内容去重；

* 原始文件保存；

* 单个采集任务内部的流水线执行；

* 向Go返回结构化结果和明确错误。



Python不负责：



* 平台级任务排队；

* 全局任务并发控制；

* 平台级任务状态机；

* 对外生产任务API；

* 将Redis作为长期业务数据库；

* 代替Go维护生产事务流程。



Python Scheduler只能协调单个采集任务内部步骤，不得扩展为平台级总调度器。



---



## 12. Go 职责



Go负责：



* 对外HTTP API；

* 任务创建；

* 任务查询；

* 任务取消；

* 平台级任务调度；

* 平台级并发控制；

* 任务状态管理；

* Python Worker调用；

* Redis队列协调；

* 临时任务状态；

* MySQL事务数据写入；

* 任务结果查询；

* 超时控制；

* 取消信号传递；

* 平台级日志和指标。



Go不负责：



* 重复实现网页下载器；

* 重复实现HTML解析器；

* 重复实现PDF或Office解析器；

* 重复实现正文清洗；

* 重复实现关键词评分；

* 重复实现内容去重；

* 将Redis队列数据直接作为永久文章结果。



如需调整 Python 与 Go 的职责边界，必须先新增或更新 ADR，并建立独立架构任务。



---



## 13. Python 通用采集流水线



目标采集流程为：



```text

任务输入

→ 输入校验

→ 站点探测

→ 生成SearchSpec

→ 执行搜索

→ 标准化搜索结果

→ URL标准化

→ URL去重

→ 下载详情页

→ 保存原始文件

→ 识别内容格式

→ HTML/PDF/Office解析

→ 正文清洗

→ 关键词匹配

→ 相关性评分

→ 内容去重

→ 生成标准文章结果

→ 返回Go

```



每个步骤必须有明确的：



* 输入；

* 输出；

* 错误；

* 日志阶段名；

* 超时；

* 测试；

* 可观测状态。



Parser不得直接写数据库。



Extractor不得直接控制平台任务状态。



下载器不得静默吞掉网络错误。



去重模块不得通过删除原始数据掩盖重复问题。



---



## 14. 搜索能力探测规则



建议的搜索探测优先级为：



```text

已有站点配置

→ 已知CMS适配器

→ HTML搜索表单

→ Sitemap

→ RSS

→ 已知搜索路径

→ 浏览器动态探测

```



浏览器探测属于后备方案。



不得默认对每个任务启动 Chromium 或 Playwright。



浏览器探测得到的搜索规则必须转换为可复用、可验证、可脱敏的 `SearchSpec`。



任何持久化的搜索规则中不得保存：



* Cookie；

* Authorization；

* Token；

* Session；

* 用户账号；

* 密码；

* 私有请求头；

* 临时签名参数；

* 其他敏感认证信息。



搜索能力探测策略在正式实施前，应与对应 ADR 保持一致。



---



## 15. API 与数据契约规则



Python 与 Go 之间必须使用明确的数据契约。



数据结构必须：



* 字段含义清晰；

* 类型稳定；

* 必填字段明确；

* 可选字段明确；

* 时间格式统一；

* URL字段含义明确；

* 错误字段结构化；

* 支持契约版本；

* 有对应测试。



不得随意：



* 重命名已有JSON字段；

* 删除已有字段；

* 改变字段类型；

* 将正常字段改成嵌套结构；

* 将错误改为空列表；

* 使用无约束的任意字典替代稳定模型；

* 在未通知调用方时改变时间或状态格式。



公共接口变更必须：



1\. 写入当前任务；

2\. 说明兼容性影响；

3\. 更新请求和响应模型；

4\. 更新调用方；

5\. 更新测试；

6\. 更新文档；

7\. 提供迁移或兼容方案。



---



## 16. 任务运行状态



平台任务状态建议统一为：



```text

pending

discovering

searching

downloading

parsing

saving

succeeded

partially\_succeeded

failed

cancelled

```



状态转换必须受控。



不得允许任务从任意状态跳转到任意状态。



需要明确：



* 合法前置状态；

* 合法目标状态；

* 状态更新时间；

* 失败原因；

* 取消原因；

* 部分成功条件；

* 重试是否产生新任务或新执行记录。



部分文章失败但仍获得有效结果时，可以返回：



```text

partially\_succeeded

```



不得把部分失败伪装成完全成功。



---



## 17. 错误处理规则



禁止使用以下方式掩盖失败：



```python

except Exception:

&#x20;   return []

```



也不得使用等效逻辑：



* 捕获所有异常后返回空对象；

* 捕获错误后返回成功状态；

* 只打印错误但不向上返回；

* 将网络失败解释为“没有搜索结果”；

* 将解析失败解释为“正文为空”；

* 将数据库失败解释为“任务完成”；

* 丢弃底层错误原因；

* 无限制重试。



错误必须尽量包含：



```text

task\_id

stage

url

error\_code

message

retryable

cause

timestamp

```



需要区分：



* 输入错误；

* 站点不支持；

* 搜索失败；

* 网络超时；

* HTTP状态错误；

* 下载失败；

* 文件损坏；

* 格式不支持；

* 解析失败；

* 内容为空；

* 数据库失败；

* Redis失败；

* Worker中断；

* 任务取消；

* 内部未知错误。



错误信息不得包含密码、Token、Cookie、Authorization或其他敏感数据。



---



## 18. 日志规则



日志必须结构化，并尽量包含：



```text

task\_id

stage

url

method

status\_code

duration\_ms

error\_code

retry\_count

timestamp

```



日志级别建议：



| 级别         | 使用场景             |

| ---------- | ---------------- |

| `DEBUG`    | 开发调试信息，不用于记录敏感正文 |

| `INFO`     | 正常任务阶段和关键状态变化    |

| `WARNING`  | 可恢复异常、降级或部分失败    |

| `ERROR`    | 当前步骤失败或任务失败      |

| `CRITICAL` | 平台无法继续提供核心服务     |



禁止在日志中记录：



* 数据库密码；

* Redis密码；

* 用户密码；

* Cookie；

* Authorization头；

* Access Token；

* Refresh Token；

* 完整私密请求头；

* 未脱敏个人敏感信息。



---



## 19. 存储规则



生产环境的数据职责原则：



```text

MySQL：事务数据的唯一事实来源

Redis：队列、协调、缓存和临时状态

文件系统或对象存储：原始HTML、PDF和Office文件

DuckDB：本地开发、测试或分析

```



禁止：



* 将Redis作为长期文章存储；

* 使用Redis `POP` 实现文章查询；

* 因查询操作消费或删除业务结果；

* 同时由多个模块无规则写入同一份事务数据；

* Parser直接写入生产数据库；

* 将大体积原始文件直接塞入普通数据库字段；

* 在没有迁移文件时直接修改生产表结构。



数据库中应主要保存：



* 文件路径或对象键；

* 文件Hash；

* 内容Hash；

* 来源URL；

* 标题；

* 发布时间；

* 来源站点；

* 命中关键词；

* 相关性评分；

* 解析状态；

* 错误信息；

* 必要元数据。



如实际代码与本节不一致，应记录现状并通过独立任务逐步迁移，不得直接进行破坏性调整。



---



## 20. 数据库变更规则



任何数据库结构变更必须：



1\. 在 `docs/TASK.md` 中明确列出；

2\. 说明影响的表、字段、索引和约束；

3\. 提供正式迁移文件；

4\. 提供升级步骤；

5\. 提供回滚方案；

6\. 检查旧数据兼容性；

7\. 检查Python和Go模型；

8\. 检查查询和写入代码；

9\. 增加迁移测试或验证步骤；

10\. 禁止直接在生产数据库手工修改后不留记录。



迁移文件一旦进入共享环境，原则上不得直接篡改历史迁移。



需要修正时，应新增迁移。



---



## 21. Redis 使用规则



Redis只能用于：



* 任务队列；

* 消息协调；

* 分布式锁；

* 限流计数；

* 临时状态；

* 有明确失效时间的缓存；

* 短期幂等键。



Redis中的临时数据必须考虑：



* TTL；

* 重复消费；

* 消息确认；

* Worker中断；

* 任务取消；

* 网络闪断；

* 数据恢复；

* 幂等性；

* 队列堆积。



不得假设Redis中的数据永久存在。



---



## 22. HTTP 客户端规则



项目中的HTTP访问必须逐步收敛到统一下载能力。



HTTP请求必须考虑：



* 连接超时；

* 读取超时；

* 总任务超时；

* 重试次数；

* 重试间隔；

* 可重试状态码；

* User-Agent；

* 编码检测；

* 重定向；

* 响应体大小限制；

* 内容类型；

* TLS错误；

* 代理配置；

* 限流；

* robots和站点使用规则；

* 取消信号。



不得：



* 无限重试；

* 没有超时；

* 无限制下载超大文件；

* 对所有错误使用同一种返回值；

* 在多个模块中复制不同的HTTP策略；

* 因本地目录名 `httpx/` 与第三方包冲突而作未经验证的直接重命名。



本地 `httpx/` 是否重命名，必须通过独立重构任务处理。



---



## 23. Parser 与 Extractor 规则



Parser负责将原始内容转换为结构化内容。



Extractor负责正文清洗、字段提取、关键词匹配和评分等内容处理。



必须遵守：



* Parser不写数据库；

* Parser不管理平台任务状态；

* Parser不吞掉格式错误；

* Parser输出结构必须稳定；

* 原始内容应可追溯；

* 清洗前后内容应能通过Hash或路径关联；

* 标题缺失、正文缺失、日期缺失必须区分；

* PDF损坏与PDF无文字必须区分；

* 不支持的格式必须返回明确错误；

* 单篇文章失败不应无条件中断整个批次；

* 批次出现部分失败时必须保留成功结果和失败明细。



---



## 24. 去重规则



需要区分：



```text

URL去重

内容去重

任务幂等

数据库唯一约束

```



URL标准化不得擅自删除可能影响资源身份的参数。



内容去重应使用稳定、可解释的方法，例如规范化内容Hash。



去重过程必须保留必要的关联信息，不得直接丢弃所有重复记录而无法追溯来源。



任务重复提交的处理策略必须由Go平台层统一控制。



---



## 25. 配置规则



配置应优先来自：



* 配置文件；

* 环境变量；

* 启动参数；

* 明确的默认值。



禁止在代码中硬编码：



* 数据库密码；

* Redis密码；

* Token；

* Cookie；

* 私有服务地址；

* 生产凭据；

* 用户机器绝对路径；

* 仅适用于某一开发者环境的配置。



可以提交：



```text

.env.example

```



不得提交真实：



```text

.env

```



示例配置必须使用虚假值或占位符。



配置变更必须说明：



* 配置名称；

* 类型；

* 默认值；

* 是否必填；

* 适用环境；

* 是否敏感；

* 向后兼容方式。



---



## 26. Python 开发规则



Python代码必须：



* 遵循项目已确定的Python版本；

* 尽量使用类型注解；

* 保持函数职责单一；

* 明确输入和返回类型；

* 使用结构化模型表达稳定数据；

* 避免循环导入；

* 避免隐式全局状态；

* 避免可变默认参数；

* 使用明确异常类型；

* 复用统一配置、日志和HTTP能力；

* 为新增行为增加测试。



不得在未确认兼容性的情况下升级关键依赖。



不得因测试环境缺失依赖而删除真实功能代码或跳过全部测试。



第三方依赖必须写入统一依赖声明，不得只在个人环境中手工安装。



---



## 27. Go 开发规则



Go代码必须：



* 通过 `gofmt`；

* 明确处理返回错误；

* 使用 `context.Context` 传播超时与取消；

* 避免无控制的goroutine；

* 避免全局混合任务状态；

* 避免数据竞争；

* 为每个 `task\_id` 独立维护状态；

* 保持handler、service和repository职责清晰；

* 对数据库事务边界进行明确控制；

* 为新增行为增加测试。



禁止：



* 忽略关键错误；

* 通过panic处理普通业务错误；

* 使用无上限goroutine；

* 使用全局计数代表单任务统计；

* 在查询接口中消费队列；

* 在Go中重复实现Python采集能力。



---



## 28. 前端开发规则



当前前端以现有工程形态为准。



除非当前任务明确要求，不得顺手将现有前端整体重建为Vue、React或其他框架。



前端调用生产API时不得硬编码：



```text

http://localhost:8080

```



生产环境优先使用：



```text

/api

```



开发环境地址应通过配置指定。



前端必须正确处理：



* 创建任务失败；

* 查询超时；

* 空结果；

* 部分成功；

* 任务失败；

* 任务取消；

* 轮询停止；

* 分页；

* URL安全展示；

* 错误原因展示。



---



## 29. 测试规则



每个任务必须执行与修改范围匹配的测试。



Python基础测试命令：



```powershell

python -m pytest -q

```



Go基础测试命令：



```powershell

cd go-spider

go test ./...

go vet ./...

```



涉及并发且当前环境支持时，再执行：



```powershell

go test -race ./...

```



涉及格式时执行：



```powershell

gofmt -w .

```



但格式化范围只能覆盖本次任务涉及的Go文件，除非任务明确授权全项目格式化。



测试要求：



* 先记录修改前基线；

* 再实施修改；

* 再执行修改后的测试；

* 记录真实命令；

* 记录真实通过数量；

* 记录真实失败数量；

* 记录跳过数量；

* 记录无法执行原因；

* 不得预填“全部通过”；

* 不得把“未执行”写成“通过”；

* 不得为通过测试而删除有效断言；

* 不得无理由跳过失败测试；

* 不得只测试理想路径。



---



## 30. 最低测试覆盖场景



与任务有关时，应覆盖：



### 正常场景



* 合法任务输入；

* 正常搜索结果；

* 正常详情页；

* 正常HTML解析；

* 正常PDF或Office解析；

* 正常关键词命中；

* 正常结果保存；

* 正常状态转换；

* 正常任务查询。



### 边界场景



* 关键词为空；

* 关键词重复；

* URL重复；

* 内容重复；

* 最大结果数为边界值；

* 标题缺失；

* 日期缺失；

* 正文为空；

* 空搜索结果；

* 部分文章失败。



### 异常场景



* URL不合法；

* 网站无搜索入口；

* HTTP 500；

* 请求超时；

* 编码异常；

* 文件损坏；

* 格式不支持；

* Redis不可用；

* MySQL不可用；

* Python Worker中断；

* 用户取消任务；

* 重复提交；

* 数据库写入失败。



---



## 31. 兼容性规则



除非任务明确允许，必须保持：



* 现有HTTP路径兼容；

* 现有JSON字段兼容；

* 现有命令行参数兼容；

* 现有配置名称兼容；

* 现有数据库兼容；

* 现有任务状态兼容；

* 现有前端调用兼容；

* 现有部署方式兼容。



如果不能完全兼容，必须：



1\. 明确列出破坏性变更；

2\. 说明受影响调用方；

3\. 提供迁移方案；

4\. 提供临时兼容层；

5\. 提供回滚方案；

6\. 获得用户确认。



---



## 32. 安全规则



不得读取、输出、提交或传播：



* 密码；

* 私钥；

* Token；

* Cookie；

* Authorization；

* 数据库真实凭据；

* Redis真实凭据；

* 用户隐私数据；

* 未脱敏生产数据。



不得执行：



* 未经确认的生产数据库操作；

* 未经确认的外部消息发送；

* 未经确认的生产部署；

* 未经确认的批量删除；

* 未经确认的数据迁移；

* 未经确认的爬取压力测试；

* 绕过网站认证或访问限制；

* 规避安全策略的操作。



抓取功能必须尊重适用法律、授权范围、网站规则和访问频率限制。



---



## 33. 文件和目录操作规则



禁止未经授权：



* 删除目录；

* 批量移动文件；

* 批量重命名；

* 覆盖用户文件；

* 清理用户工作区；

* 删除历史迁移；

* 删除测试；

* 删除兼容模块；

* 删除归档资料；

* 移动论文资料；

* 修改 `workspace/crawler` 之外的项目。



需要重构目录时，必须单独建立 `refactor` 类型任务，并说明：



* 旧路径；

* 新路径；

* import影响；

* 配置影响；

* 测试影响；

* 部署影响；

* 兼容重导出；

* 回滚步骤。



---



## 34. 禁止事项



AI不得：



1\. 未读取 `docs/TASK.md` 就修改代码；

2\. 未查看真实实现就给出确定性修复结论；

3\. 用架构设想代替当前代码事实；

4\. 超出当前任务范围顺手修改；

5\. 隐藏测试失败；

6\. 虚构测试结果；

7\. 用空列表掩盖异常；

8\. 静默改变公共接口；

9\. 静默改变数据库结构；

10\. 静默改变Python与Go职责；

11\. 擅自删除旧模块；

12\. 擅自进行大规模目录迁移；

13\. 擅自重建前端工程；

14\. 默认引入Playwright或Chromium；

15\. 默认启动浏览器完成所有搜索；

16\. 把Redis当作长期文章数据库；

17\. 通过查询操作消费和删除文章结果；

18\. 将原始大文件直接无规则写入数据库；

19\. 提交真实密钥或环境文件；

20\. 覆盖用户已有未提交修改；

21\. 使用破坏性Git命令清理工作区；

22\. 在未验证时声称任务已经完成。



---



## 35. AI 工作过程要求



AI执行任务时，应按以下顺序工作：



```text

读取规则

→ 读取当前TASK

→ 检查Git状态

→ 检查真实代码

→ 确认调用链

→ 记录修改前基线

→ 提出最小实施方案

→ 在允许范围内修改

→ 执行针对性测试

→ 执行完整相关测试

→ 检查git diff

→ 更新TASK执行记录

→ 汇报结果和遗留问题

```



工作过程中应持续说明：



* 当前检查了什么；

* 发现了什么；

* 准备修改什么；

* 为什么需要修改；

* 测试是否执行；

* 是否出现阻塞。



不得只给出模糊结论，例如：



```text

已优化

已修复

测试正常

代码没问题

```



必须提供能够核对的文件、命令、结果和影响范围。



---



## 36. 代码修改原则



所有修改应遵循：



```text

最小修改

职责清晰

接口稳定

错误透明

结果可测试

过程可回滚

```



优先修复根因，不通过堆叠特殊分支掩盖设计问题。



但如果根因修复明显超出当前任务范围，应先完成安全、兼容的当前任务方案，再建立独立后续任务。



不得为了“代码更漂亮”修改与当前任务无关的代码。



---



## 37. 文档同步规则



如果代码修改影响以下内容，必须同步更新对应文档：



| 变更内容       | 应更新文档                           |

| ---------- | ------------------------------- |

| 系统职责或数据流   | `docs/SYSTEM\_ARCHITECTURE.md`   |

| 长期开发规则     | `docs/DEVELOPMENT\_RULES.md`     |

| 当前任务实施和测试  | `docs/TASK.md`                  |

| 重要架构决策     | `docs/decisions/ADR-*.md`       |

| 安装、启动和使用方式 | `README.md`                     |

| 配置项        | `README.md`、`.env.example`或配置文档 |

| 公共API      | API文档及相关模型说明                    |

| 数据库结构      | 迁移文件和数据模型文档                     |



不要把一次性执行记录写进长期架构规则。



不要把长期架构约束只写在某一次 `TASK.md` 中。



---



## 38. ADR 规则



以下变化必须考虑新增 ADR：



* Python与Go职责边界变化；

* 事实数据源变化；

* Redis用途变化；

* 搜索探测优先级变化；

* 数据契约版本策略变化；

* 任务状态机变化；

* 原始文件存储方式变化；

* 核心目录结构变化；

* 核心框架或基础设施替换。



ADR状态允许使用：



```text

proposed

accepted

superseded

deprecated

rejected

```



已接受的ADR不得直接改写成相反结论。



需要改变时，应新增ADR，并在旧ADR中注明被哪个新ADR替代。



---



## 39. 任务完成前检查



任务结束前必须执行：



```powershell

git status --short

git diff --stat

git diff

```



并检查：



* 是否只修改了任务允许的文件；

* 是否存在意外格式化；

* 是否存在无关改动；

* 是否误提交敏感信息；

* 是否误改公共接口；

* 是否误改数据库结构；

* 是否补充必要测试；

* 是否记录真实测试结果；

* 是否更新必要文档；

* 是否仍存在阻塞问题；

* 是否满足验收标准；

* 是否可以安全回滚。



如果有任何验收项未满足，不得直接标记任务为 `completed`。



---



## 40. 最终汇报格式



任务完成后，AI应按照以下结构汇报：



```text

1\. 完成结果

2\. 修改文件

3\. 核心变更

4\. 实际执行的测试

5\. 测试结果

6\. 未通过或未执行内容

7\. 兼容性影响

8\. 数据库和配置影响

9\. 遗留问题

10\. 建议的下一步

```



测试结果必须使用真实输出。



如果任务被阻塞，应说明：



```text

阻塞位置

阻塞原因

已完成部分

未完成部分

需要用户决定的内容

继续执行所需条件

```



---



## 41. 当前项目特别约束



当前通用化升级期间，必须遵守：



1\. 先建立运行和测试基线；

2\. 再修复基础契约和明显错误；

3\. 再统一HTTP下载能力；

4\. 再建立标准数据模型；

5\. 再实现HTML搜索插件；

6\. 再实现浏览器后备探测；

7\. 再串联Python采集流水线；

8\. 再完善Go任务API、调度和持久化；

9\. 再完善前端任务表单和结果展示；

10\. 最后进行端到端验收和旧模块收敛。



不得跳过测试基线，直接开始大范围通用化重构。



当前第一个任务应为：



```text

TASK-001：建立项目运行与测试基线

```



TASK-001期间不得顺手修改：



* 搜索业务逻辑；

* 浏览器探测；

* 数据库结构；

* Python与Go通信协议；

* 前端功能；

* 采集流水线；

* 目录结构；

* 生产任务状态机。



---



## 42. 当前目录特别说明



以下两个目录不是同一个项目：



```text

E:\\AI\_Projects\\Codex\\workspace\\crawler\\go-spider

E:\\AI\_Projects\\Codex\\workspace\\go-spider

```



其中：



```text

workspace/crawler/go-spider

```



是本通用采集平台内的正式Go服务。



而：



```text

workspace/go-spider

```



是总仓库中的独立试验项目或历史原型，除非当前任务明确授权，不得修改、合并或删除。



---



## 43. 最终原则



任何参与本项目的AI或开发者，都必须遵守以下底线：



```text

先确认任务，再检查代码；

先记录基线，再实施修改；

只修改授权范围；

不掩盖错误；

不虚构测试；

不破坏兼容性；

不覆盖用户修改；

不泄露敏感信息；

所有重要修改必须可验证、可追踪、可回滚。

```



